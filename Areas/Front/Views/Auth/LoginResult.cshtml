@using Bonsai.Areas.Front.ViewModels.Auth
@model Bonsai.Areas.Front.ViewModels.Auth.AuthStatus
@{
    ViewBag.Title = "Авторизация";
    Layout = "~/Areas/Front/Views/Layout.cshtml";
}

<div class="text-page">
    <h1>Авторизация</h1>

    @if(Model == AuthStatus.Failed)
    {
        <div class="alert alert-danger">
            <b>Ошибка</b>
            <p>Не удалось авторизоваться.</p>
        </div>
    }
    else if(Model == AuthStatus.LockedOut)
    {
        <div class="alert alert-danger">
            <b>Ошибка</b>
            <p>Ваша учетная запись заблокирована администратором.</p>
        </div>
    }
    else if (Model == AuthStatus.Unvalidated)
    {
        <p>Ваша учетная запись еще не была проверена администратором.</p>
        <p>Пожалуйста, дождитесь проверки - тогда вы сможете авторизоваться.</p>
    }
</div>